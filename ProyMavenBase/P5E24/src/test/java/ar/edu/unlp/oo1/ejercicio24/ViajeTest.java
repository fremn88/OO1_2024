package ar.edu.unlp.oo1.ejercicio24;

public class ViajeTest {

	/*
	 * test procesar viaje: se da de alta un viaje (conductor). Al viaje se le agregan pasajeros (pasajero se registra).
	 * A)verifica que a cada pasajero y al conductor se le descuenta el saldo.
	 * B)verifica que al vehiculo del viaje se le restaura la ocupacion
	 * 
	 * particiones:
	 * A) pasajeros con: saldo>costo y  saldo<costo / idem conductor
	 * B) viaje con ocupacion: llena / parcialmente llena / vacio solo conductor
	 */
}
